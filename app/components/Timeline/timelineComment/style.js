import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  inputContainer: {
    borderRadius: 2,
    paddingTop: 16,
    paddingBottom: 16
  },
  comment: {
    paddingTop: 16,
    borderWidth: 1,
    paddingBottom: 16,
  },
})

export default styles;
